name: qr_scanner_reader
description: This is the ultimate Qr Scanner/Reader of Apphile crew
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.16.0
  flutter_staggered_grid_view: ^0.4.0
  gallery_saver: ^2.1.2
  get: ^4.1.3
  get_storage: ^2.0.2
  permission_handler: ^8.1.2
  qr_code_scanner: ^0.5.1
  qr_flutter: ^4.0.0

dev_dependencies:


flutter:
  uses-material-design: true
  assets:
    - assets/icons/
    - assets/images/
    - assets/anims/
